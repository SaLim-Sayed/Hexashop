/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "www.pexels.com",
            "i.dummyjson.com"
        ],
       
    }
}

module.exports = nextConfig
